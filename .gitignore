# ── 공통 ─────────────────────────────────────
.env
.idea/
.vscode/
*.iml
out/

# ── 백엔드 (Gradle) ──────────────────────────
.gradle/
.gradle-user-home/
build/
shared/backend/.gradle/
shared/backend/build/
shared/backend/.env
# 처방전 이미지 저장 위치 (PrescriptionImageStorage)
uploads/
shared/backend/uploads/

# ── AI 서버 (Python) ─────────────────────────
shared/ai/.venv/
# 파이썬 캐시는 어디에 생기든 넣지 않는다.
# scripts/ 아래 것이 추적되고 있어서 시드를 돌릴 때마다 변경으로 잡혔다
__pycache__/
*.pyc
shared/ai/.pytest_cache/
shared/ai/chroma_db/
shared/ai/tessdata/
shared/ai/.env
# 약 이름 매칭 로그 (match_log.jsonl)
shared/ai/data/

# ── 프론트엔드 (앱·웹 공통) ──────────────────
.pnpm-store/
node_modules/
dist/
